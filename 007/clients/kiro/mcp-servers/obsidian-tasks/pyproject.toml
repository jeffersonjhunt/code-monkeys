[project]
name = "obsidian-tasks"
version = "0.1.0"
description = "MCP server for querying and managing Obsidian-style markdown tasks"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]==1.9.2",
]
