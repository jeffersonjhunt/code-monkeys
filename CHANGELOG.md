# Changelog

Reverse-chronological log of material changes. Append a dated entry whenever a phase completes, a decision changes, or production state changes.

## 2026-05-07

- **Enabled prefix caching** (`--enable-prefix-caching`) on both replicas. vLLM emits an experimental warning for hybrid Mamba/attention models (`Prefix caching in Mamba cache 'align' mode is currently enabled. Its support for Mamba layers is experimental.`) but functioned correctly. KV pool shrunk slightly (1,497,029 → 1,379,705 tokens; 42.11x concurrency at 32K context vs prior 45.69x) — the difference is metadata overhead, not a meaningful loss.
- **Bench through HAProxy was basically unchanged** (~within noise of the prefix-caching-off run): 38 → 76 → 157 → 280 → 219 → 672 tok/s. Two tail-latency spikes (23s at c=1, 74s at c=16) showed up in this run but likely reflect HAProxy round-robin picking different cache states across replicas, not a regression. Decode-dominated workloads see no benefit because the bench prompts are tiny.
- **Where prefix caching actually helps — TTFT on long prefixes.** A targeted 4K-token system-prompt test (`max_tokens=1` to isolate prefill) showed **4× TTFT speedup** cold-vs-warm: 736 ms → 183 ms (75 % TTFT reduction), confirmed with two independent prefixes. `vllm:prefix_cache_hits_total` metrics also confirmed live cache hits at ~49 % rate. This is the realistic win for OpenCode-style agent traffic with repeated system prompts and multi-turn history.
- Caveat for HAProxy round-robin: each replica owns an independent prefix cache. A multi-turn conversation that bounces between replicas re-warms each one separately. If sticky sessions matter (heavy long-context agent), revisit HAProxy `balance source` or per-conversation routing — currently `roundrobin`, fine for stateless completion traffic.
- Updated `src/compose/vllm/compose.yml` (added `--enable-prefix-caching` flag) and `docs/runbook.md`.

## 2026-05-06

- **Track A round 4 — `QuantTrio/Qwen3.6-35B-A3B-AWQ` works on the cluster.** Community AWQ of Qwen3.6-35B-A3B (DeltaNet hybrid + MoE, 35B total / 3B active). Per the QuantTrio model card, set `VLLM_USE_DEEP_GEMM=0`, `VLLM_USE_FLASHINFER_MOE_FP16=1`, `VLLM_USE_FLASHINFER_SAMPLER=0` to dodge the broken kernel paths on sm_120. vLLM auto-selected `awq_marlin` (the goal — bypasses the FlashInfer NVFP4 fused-MoE that's been the recurring blocker), used `FlashInfer CUTLASS Unquantized MoE backend` for the unquantized layers, Triton/FLA GDN prefill for the DeltaNet attention, FLASH_ATTN for standard attention. Loaded in 22.23 GiB GPU memory at `gpu_memory_utilization=0.50` (Spark UMA needs the clamp). torch.compile 31s, warmup ~50s, CUDA graph capture ~30s. **First MoE model that actually serves on DGX Spark in this project.**
- Smoke test PASS on both `starsky:8000` and `hutch:8000`. Tool calling **auto mode works reliably** with `--tool-call-parser qwen3_coder --reasoning-parser qwen3` — sent a weather-tool prompt and got clean structured `tool_calls[]` with `finish_reason: "tool_calls"` (the `<think>` content lands in `.reasoning` per the qwen3 parser, the actual answer/tool calls in `.content` / `.tool_calls`). This is the major reliability win over Qwen2.5-Coder + hermes parser.
- **Bench sweep (cluster via `starsky:8080`, c=1..32, after both replicas were on the corrected compose):** 38 → 76 → 160 → 275 → 469 → 752 tok/s aggregate. Single-replica peak earlier was 449 tok/s at c=16; cluster peak 752 tok/s at c=32 = **~13× the previous Qwen2.5-Coder cluster baseline (~56 tok/s/replica × 2)**. Per-stream: 38 tok/s at c=1 (vs ~3.7 tok/s on Qwen2.5 = 10×), tapering to 26 tok/s/stream at c=32 — healthy continuous-batching scaling.
- **Compose drift fix:** discovered `--swap-space` was removed from `vllm serve` in v0.20.x (vLLM auto-tunes KV/swap now). Starsky had been running a 10-hour-old container with an older command line that didn't include the flag, so it kept working — but the current compose.yml had it, and hutch's fresh deploy failed `unrecognized arguments: --swap-space 16`. Removed the two lines from `src/compose/vllm/compose.yml`, redeployed both. `--max-num-seqs 32`, `--trust-remote-code`, `--reasoning-parser qwen3` all still valid in this image.
- **Cross-box cache transfer worked as designed:** `ssh starsky 'tar -C /srv/models -cf - models--QuantTrio--Qwen3.6-35B-A3B-AWQ' | ssh hutch 'tar -C /srv/models -xpf -'` (with agent forwarding so starsky→hutch goes direct, not through the workstation). 23.71 GiB transferred in 14m23s. Confirms the runbook procedure for cluster model swaps.
- **Smoke-test script update:** with `--reasoning-parser qwen3` enabled, low max_tokens (16) caused `.content=null` because the model used all the budget on `<think>`. Bumped to 256, added `chat_template_kwargs.enable_thinking: false`, and made the script print `.content` if populated else fall back to `.reasoning`.
- **Runbook update:** rewrote the Tool calling section to reflect the current Qwen3.6 + `qwen3_coder` parser (auto mode reliable), kept the Qwen2.5-Coder + hermes caveat as a "legacy" entry for context.
- Triaged vLLM issue #41645 (jhunt asked if it was our bug) — it isn't. Different model family (`openai/gpt-oss`), different quant (MXFP4 not NVFP4), datacenter Blackwell (sm_100) not DGX Spark (sm_120), and TP > 1 not TP=1. Fix targets v0.20.2 milestone via PR #41646, but the issue itself notes NVFP4 paths return correct shapes — so the fix doesn't reach our `cutlass TMA WS grouped gemm` failure. Useful as a cadence signal: v0.20.2 is the active Blackwell + MoE release, worth retrying our stack against. Updated `docs/parking-lot.md` retry triggers accordingly.
- Track A round 3: tried `RedHatAI/Qwen3-30B-A3B-NVFP4` via NIM model-free 2.0.3 — smaller model (~17 GB NVFP4), pure MoE (no hybrid Mamba), to see if dropping the Mamba layers and reducing size dodges the crash. **Same exact failure mode**: every cutlass TMA WS grouped-GEMM tactic (14, 15) at sm_120 fails with `Error Internal`. Inside the container the engine process loops silently (load weights → autotune fail → restart, without bumping docker's RestartCount), looking superficially like progress for ~10 min before we caught it. Conclusively pins the bug to **FlashInfer's `fused_moe_120.so` NVFP4 kernel pack**, universal across NVFP4 MoE models on DGX Spark — not Mamba-specific, not size-specific, not Qwen3-Coder-Next-specific. Cleanup: stopped NIM, restarted vLLM on starsky to restore the cluster. Updated `docs/parking-lot.md` with the FlashInfer pinpoint and a new pre-retry workaround idea: force a non-FlashInfer MoE backend (`VLLM_CUTLASS`, `MARLIN`, or `EMULATION` from vLLM's logged alternatives) on the next attempt.

## 2026-05-05

- Project initialized. Architecture: vLLM replicas (not sharded) on starsky + hutch, HAProxy on starsky as primary LB, containerized via Docker, target model `RedHatAI/Qwen3-Coder-Next-NVFP4`. ConnectX-7 direct link reserved for future sharded mode.
- Initial docs scaffolding created: `CLAUDE.md`, `README.md`, `TASKS.md`, `CHANGELOG.md`, `docs/`, `src/`.
- SSH connectivity verified for `jhunt@starsky` and `jhunt@hutch`. Both boxes confirmed `aarch64` with `NVIDIA GB10`. Domain: `tworivers`. Hutch host key accepted on first connect.
- Phase 1 discovery: `src/scripts/preflight.sh` authored and executed against both boxes. Outputs saved to `docs/inventory-starsky.md` and `docs/inventory-hutch.md`; distilled findings in `docs/inventory-summary.md`. Both boxes are functionally identical (Ubuntu 24.04, kernel 6.17.0-1014-nvidia, ARM64, GB10 GPU, driver 580.142 / CUDA 13.0, Docker 29.2.1 + nvidia-container-toolkit 1.19.0). `jhunt` is in the `docker` group on both. ConnectX-7 link is up at L1 but not configured for inter-host L3 — non-blocker for replica mode. No driver/toolkit installation needed in Phase 3.
- Phase 2 scaffolding (initial): Ansible inventory, `group_vars/all.yml`, `site.yml`, and roles `common`/`vllm`/`haproxy` under `src/ansible/`. Compose and HAProxy templates under `src/compose/` and `src/configs/`. Image research confirmed `vllm/vllm-openai:v0.20.1-cu129-ubuntu2404` as the right tag (linux/arm64 manifest + CUDA 12.9 runtime, forward-compatible with the box's CUDA 13.0 driver, exceeds vLLM 0.14.1+ minimum from the model card). HF model card lookup: `Qwen3-Coder-Next` is 80B total / 3B active MoE → NVFP4 weights ≈ 40 GB, fits one Spark with headroom. Decision: TP=1 per replica (overrides the model card's TP=2 datacenter-validated config). Tool-calling flags (`--enable-auto-tool-choice`, `--tool-call-parser qwen3_coder`) per model card requirements.
- Phase 2 reworked: dropped Ansible at user's pushback — for 2 hosts the abstraction had no leverage. Replaced with plain compose stacks under `src/compose/{vllm,haproxy}/` (compose files are now the source of truth, no templating layer), `src/scripts/bootstrap.sh` for one-time host prep, and `src/scripts/deploy.sh` for `rsync + docker compose up -d`. All technical decisions (image tag, TP=1, tool-calling flags) carried over unchanged. See `docs/decisions.md` for the rationale. `.gitignore` updated to gitignore `src/compose/vllm/.env` while keeping `.env.example` committed.
- Phase 3 base provisioning: `bootstrap.sh` ran clean on both boxes. `/srv/models` created (mode 0755, owned by jhunt). `/etc/hosts` block-managed entries added on both initially. Cross-resolution verified (`getent ahosts`) and L3 connectivity confirmed (`ping`: starsky→hutch 1.53 ms, hutch→starsky 0.885 ms over the LAN segment). No errors, no degraded output — both boxes now ready for the vLLM stack.
- Phase 3 follow-up: removed the `/etc/hosts` management at user's pushback. Verified DNS already resolves both `starsky`/`starsky.tworivers` and `hutch`/`hutch.tworivers` cleanly from each box (`host` command, which bypasses `/etc/hosts`). Since nsswitch order is `files mdns4_minimal [NOTFOUND=return] dns`, the manual entries were silently shadowing DNS — a latent debugging trap if IPs ever changed. Updated `bootstrap.sh` to remove the block (with backward-compat cleanup for hosts where the old version ran), switched `haproxy.cfg` backends from IPs to hostnames so DNS is the single source of truth, and re-ran `bootstrap.sh` on both boxes to clean up the previously-added entries.
- Phase 5 (Track D) **complete**: `Qwen/Qwen2.5-Coder-32B-Instruct` healthy on both replicas. Watcher fired at 18:04:30 UTC; smoke tests PASS direct against starsky:8000 and hutch:8000 (response: "hello" as instructed). `smoke-test.sh` updated to auto-discover the served model id from `/v1/models` so it never silently desyncs from the .env. The earlier "404 on /v1/chat/completions" was the smoke-test sending a hardcoded `qwen3-coder-next` model name, not a vLLM problem.
- Phase 6 (HAProxy) **complete**: deployed on starsky with `:80` initially, but `haproxy:alpine` runs as the `haproxy` user and can't bind privileged ports under `network_mode: host` — got `Permission denied`. Switched to `0.0.0.0:8080` (no caps needed). Found a second trap: `docker compose up -d` doesn't recreate when only bind-mounted files change, so port-80 → port-8080 didn't take effect until force-recreate. Hardened `deploy.sh` to always pass `--force-recreate`. After fix: both backends UP in `:8404/stats`, six round-robin requests served correctly through `starsky:8080`, failover drill clean (stopped hutch's vllm → HAProxy marked DOWN within 30 s → 4 requests via starsky alone → restart → rejoin).
- Phase 7 (persistence) partial: log rotation added to both compose stacks (vLLM 3×100 MB, HAProxy 3×20 MB). vLLM in-container healthcheck rewritten to use `python3` urllib instead of `wget` (the vllm-openai image doesn't ship wget — that was the source of the misleading `(unhealthy)` status; healthcheck takes effect on next vLLM redeploy). Comprehensive runbook in `docs/runbook.md` covering health checks, start/stop/restart, model swap (incl. cross-box `tar | ssh tar` transfer to skip re-downloading from HF), HAProxy reload, failover drill, reboot survival, log access, and teardown.
- Track A round 1 — `nvcr.io/nim/nvidia/llm-nim:latest` (NIM 1.15.5): wrong image. Generic NIM LLM at this tag has zero GB10 profiles (verified via `list-model-profiles`). User correctly pointed out that `model-free-nim:2.0.3` is the right image for the BYO/Spark path. Switched.
- Polish phase: re-enabled tool calling (`--enable-auto-tool-choice --tool-call-parser hermes`) and parked NIM exploration. Tested `tool_choice: "required"` (reliable — vLLM uses guided decoding to force the schema; `tool_calls[]` populated cleanly) vs `tool_choice: "auto"` (unreliable — Qwen2.5-Coder-32B-Instruct sometimes emits `<tools>` plural or bare JSON instead of `<tool_call>` singular, and the hermes parser correctly drops those). Tried mounting `tool_chat_template_hermes.jinja` from vLLM examples — made auto worse (model emitted bare JSON with no tags), reverted. Documented the caveat in `docs/runbook.md` §Tool calling with OpenCode integration guidance. NIM compose stack and findings consolidated in `docs/parking-lot.md` with retry triggers and steps.

- Track A round 2 — `nvcr.io/nim/nvidia/model-free-nim:2.0.3` with `NIM_MODEL_PATH=hf://RedHatAI/Qwen3-Coder-Next-NVFP4`: **same upstream gap as stock vLLM, lower in the stack.** Container ran further than 1.15.5 — auto-detected UMA and clamped `gpu_memory_utilization 0.9 → 0.5`, downloaded weights (45 GB), loaded all 10 safetensors shards in ~4 min. Crashed during engine init at `[TensorRT-LLM][ERROR] Assertion failed: Failed to initialize cutlass TMA WS grouped gemm` — the fused-MoE path for NVFP4 quantized models. Notable: NIM 2.0.3 *does* ship sm_120 cutlass kernels (`cutlass_instantiations/120/gemm_grouped/120/...sm120...generated.cu`), but all tactics the FlashInfer autotuner tried failed at runtime with "Error Internal." Same fundamental NVFP4-MoE-on-sm_120 hardware/kernel gap that bit stock vLLM 0.20.1 — neither path is viable for `Qwen3-Coder-Next-NVFP4` on DGX Spark today. Cleaned up: stopped NIM, restarted vLLM on starsky. Conclusion: cluster stays on vLLM with `Qwen2.5-Coder-32B-Instruct`. Per-model `*-dgx-spark` NIMs (e.g. `qwen3-32b-dgx-spark`) presumably work since NVIDIA pre-builds them with hand-validated kernel paths, but those don't include a coder-tuned variant — revisit if a coder NIM ships or the cutlass sm_120 grouped-GEMM kernels get fixed upstream.
- Phase 5 attempt 1 — **blocked** by upstream vLLM bugs on DGX Spark (sm_120) with NVFP4 MoE + hybrid Mamba/attention. `deploy.sh` worked end-to-end (after switching from rsync → `tar | ssh tar` since rsync isn't on the workstation), image pulled cleanly on both boxes, weights downloaded (44 GB on starsky), and vLLM loaded the model into 44.31 GiB GPU memory. But during torch.compile / CUDA-graph capture, vLLM crashed with `cudaErrorNoKernelImageForDevice` — the binary lacks sm_120 kernels on the NVFP4 MoE path. Open vLLM issues map directly to our config: #41477 (Triton MXFP4 MoE uses Hopper-only PTX, fails on GB10), #41564 (`/wake_up` errors on hybrid-Mamba/SWA models, sm_120). v0.20.1 (released 2026-05-04) is the latest; no newer tag exists. Containers stopped on both boxes; cached weights on `/srv/models` retained. `deploy.sh` updated to use `tar | ssh tar` and to propagate the script's exit code through the wrapper. `src/scripts/smoke-test.sh` written for use once a working stack is in place.
